# Third-party notices

## Direct JavaScript dependencies

- React、React DOM、OpenCC JS、Vite、`@vitejs/plugin-react` 和 React/Node 类型包：MIT。
- Lucide React：ISC。
- TypeScript：Apache-2.0。
- `@tauri-apps/api`、`@tauri-apps/cli`：MIT OR Apache-2.0。

## Direct Rust dependencies

- Tauri、Tauri Build、base64、futures-util、reqwest、serde、serde_json 和 uuid：MIT OR Apache-2.0。
- Tokio、tokio-tungstenite：MIT。

准确安装版本及完整传递依赖树以 `package-lock.json` 和 `src-tauri/Cargo.lock` 为准。第三方组件保留各自的版权和许可证；本仓库的作品审阅限制不会替代或缩减第三方许可证授予的权利。

DashScope 和任何 OpenAI-compatible Provider 都是外部服务；使用者必须遵守对应服务条款和数据处理政策。
